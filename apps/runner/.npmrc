# Force pnpm to use hoisted (flat) node_modules structure
# This ensures OpenTelemetry packages are hoisted to node_modules/@opentelemetry/
# so the vendored Sentry packages can find them
node-linker=hoisted
