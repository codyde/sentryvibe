{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "setup": {
      "commands": [
        "cd ../.. && pnpm install --filter=sentryvibe-broker..."
      ]
    }
  },
  "deploy": {
    "startCommand": "cd apps/broker && pnpm dev"
  }
}
