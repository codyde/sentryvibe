{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "setup": {
      "inputs": ["."],
      "commands": [
        "pnpm install --filter=@sentryvibe/agent-core --filter=sentryvibe-broker"
      ]
    },
    "compile": {
      "inputs": ["$setup"],
      "commands": [
        "pnpm --filter=sentryvibe-broker run build"
      ]
    }
  },
  "deploy": {
    "aptPackages": ["nodejs"],
    "startCommand": "node broker/dist/index.js",
    "inputs": ["$setup", "$compile"]
  }
}
