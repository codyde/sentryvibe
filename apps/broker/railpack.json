{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "setup": {
      "commands": [
        "pnpm install --filter=@sentryvibe/agent-core --filter=sentryvibe-broker"
      ]
    },
    "compile": {
      "inputs": ["$setup"],
      "commands": [
        "pnpm --filter=sentryvibe-broker run build"
      ]
    }
  },
  "deploy": {
    "base": {
      "image": "node:22-slim"
    },
    "startCommand": "node apps/broker/dist/index.js",
    "inputs": ["$compile"]
  }
}
