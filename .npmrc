# Enable dependency hoisting for Next.js compatibility
# This resolves module resolution issues with Next.js internal dependencies
# such as 'next-flight-client-entry-loader' and '@swc/helpers'
shamefully-hoist=true